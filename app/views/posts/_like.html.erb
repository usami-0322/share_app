<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :post_id, post.id %></div>
  <%= button_tag(class: "btn btn-default") do %>
    <%= content_tag :span, post.liked_by.count, class: "fa fa-heart like-btn" %>
  <% end %>
<% end %>