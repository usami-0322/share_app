<% provide(:title, @title)%>
<h1><%= @title %></h1>
<div class="row">
  <div class="search_form">
    <%= render "users_search_form" %>
  </div>
</div>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate %>
